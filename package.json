{
  "name": " ",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "build-dev": "parcel build index.html && parcel index.html --open",
    "start-front": "npm run build && npm run dev",
    "start-server": "nodemon ./server/app.js localhost",
    "start-both": "concurrently --kill-others \"npm run build-dev\" \"npm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.0.2",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "sass": "^1.32.11"
  }
}
